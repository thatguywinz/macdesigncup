/** Whether the visitor is on the Enter door (hovered or focused): EnterDoor
 *  sets it, and the live scene reads it every frame and lets the portal burn
 *  a little brighter. */
export const DOOR_LIT = { on: false };
